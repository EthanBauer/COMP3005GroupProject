Direction: 
1. clone the repo
2. run python queries.py from the submission folder 
optional steps if repopulating the database is needed:
4. copy the content from tables.sql to postgres query tool to initialize the tables
5. please modify the postgres connection parameters and statsbomb/open-data folder locations inside the json loaders stored in submission/json_loader.
6. follow the instruction from OMP3005-Project-Template (https://github.com/gabrielmartell/COMP3005-Project-Template?tab=readme-ov-file) to export the db.

Other information:
sql commands and queryies are stored in submission/sqls
bonus query video can be found at:
https://youtu.be/l66Z_Y4QEi0?feature=shared
